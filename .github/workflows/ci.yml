name: CI

on: [push]

jobs:
  build:

    runs-on: windows-2022

    steps:
      - name: Setup Windows SDK
        uses: GuillaumeFalourd/setup-windows10-sdk-action@v1
        with:
          sdk-version: 18362

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v1.9.0
        with:
          dotnet-version: '6.0.x'

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.1

      - name: Setup VSTest
        uses: darenm/Setup-VSTest@v1

      - name: Checkout Code
        uses: actions/checkout@v2

 #     - name : Setup nuget
 #       uses: nuget/setup-nuget@v1
 #         with:
 #           nuget-api-key: ${{ secrets.NUGET_API_KEY }}
 #           nuget-version: '5.x'


      - name: Restore nuget packages
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\MQTTnet.sln /t:Restore /p:Configuration="Release" /verbosity:m

      - name: Build MQTTnet core library
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\Source\MQTTnet\MQTTnet.csproj /t:Build /p:Configuration="Release" /verbosity:m

      - name: Build Managed Client extension library
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\Source\MQTTnet.Extensions.ManagedClient\MQTTnet.Extensions.ManagedClient.csproj /t:Build /p:Configuration="Release" /verbosity:m

      - name: Build RPC extension library
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\Source\MQTTnet.Extensions.Rpc\MQTTnet.Extensions.Rpc.csproj /t:Build /p:Configuration="Release" /verbosity:m

      - name: Build WebSocket4Net extension library
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\Source\MQTTnet.Extensions.WebSocket4Net\MQTTnet.Extensions.WebSocket4Net.csproj /t:Build /p:Configuration="Release" /verbosity:m

      - name: Build ASP.NET Core extension library
        #working-directory: Source
        #run: msbuild MQTTnet.sln /t:Clean /t:Restore /t:Build /p:Configuration="Release" /p:FileVersion=$assemblyVersion /p:AssemblyVersion=$assemblyVersion /p:PackageVersion=$nugetVersion /verbosity:m
        run: msbuild .\Source\MQTTnet.AspNetCore\MQTTnet.AspNetCore.csproj /t:Build /p:Configuration="Release" /verbosity:m

      #- name: Core Tests
      #  run: vstest.console.exe /Platform:x64 .\Tests\MQTTnet.Core.Tests\MQTTnet.Tests.csproj

      #- name: ASP.NET Tests
      #  run: vstest.console.exe /Platform:x64 .\Tests\MQTTnet.AspNetCore.Tests\MQTTnet.AspNetCore.Tests.csproj

    #  - name: Build
    #    shell: pwsh
    #    run: |
    #      Set-Location -Path "Build"
    #      .\build.ps1 1.2.3 4.5.6-rc1

      - name: Collect nuget Packages
        uses: actions/upload-artifact@v2
        with:
          name: nuget Packages
          path: |
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet\bin\Release\MQTTnet.*.nupkg
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet\bin\Release\MQTTnet.*.snupkg
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet.Extensions.ManagedClient\bin\Release\MQTTnet.Extensions.ManagedClient.*.nupkg
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet.Extensions.ManagedClient\bin\Release\MQTTnet.Extensions.ManagedClient.*.snupkg
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet.Extensions.RPC\bin\Release\MQTTnet.Extensions.Rpc.*.nupkg
            D:\a\MQTTnet\MQTTnet\Source\MQTTnet.Extensions.RPC\bin\Release\MQTTnet.Extensions.Rpc.*.snupkg

#      - name: Install dependencies
#        run: dotnet restore MQTTnet.sln
#      - name: Build
#        run: dotnet build --configuration Release --no-restore MQTTnet.sln
#      - name: Test MQTTnet
#        run: dotnet test --no-restore --verbosity normal Tests\MQTTnet.Core.Tests\MQTTnet.Tests.csproj
#      - name: Test AspNetCore
#        run: dotnet test --no-restore --verbosity normal Tests\MQTTnet.AspNetCore.Tests\MQTTnet.AspNetCore.Tests.csproj